# Optional overrides for Watchtower (compose/watchtower).
# Default poll is 60s via compose.yml — no .env required to start.
#
# WATCHTOWER_POLL_INTERVAL=300
# WATCHTOWER_NOTIFICATION_URL=discord://token@id
