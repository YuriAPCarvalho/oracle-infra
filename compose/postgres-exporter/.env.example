# Created on the VPS only. Never commit real credentials.
# Prefer: bash scripts/postgres-exporter-seed-env.sh
#
# DATA_SOURCE_NAME=postgresql://USER:PASSWORD@postgres:5432/postgres?sslmode=disable
DATA_SOURCE_NAME=
