# Secrets reais ficam somente na VPS — nunca commitados.
# Copiar para /opt/infra/compose/gold-api/.env na implantação.

SERVICE_NAME=gold-api
SERVICE_IMAGE=ghcr.io/scriptsgoldtw/gold-api:latest
SERVICE_HOST=scriptgold.com.br

NODE_ENV=production
PORT=3000
ENABLE_SWAGGER=false
WHATSAPP_AUTH_PATH=/data/auth_info

DATABASE_URL=
ENCRYPTION_IV=
ENCRYPTION_KEY=
JWT_SECRET=
PASSWORD_ADMIN=
TELEGRAM_TOKEN=
