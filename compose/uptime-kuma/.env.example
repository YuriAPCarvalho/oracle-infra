# Discord alerts for Uptime Kuma monitors (DOWN / UP only).
# Do NOT reuse the CI/CD Discord webhook.
# Copy to .env on the VPS and fill the URL (chmod 600). Never commit .env.

KUMA_DISCORD_WEBHOOK_URL=
