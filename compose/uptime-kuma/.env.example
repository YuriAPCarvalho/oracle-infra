SERVICE_HOST=uptimekuma.marca7.tech

# Discord alerts for monitor DOWN/UP (VPS only — never commit the real URL)
# KUMA_DISCORD_WEBHOOK_URL=https://discord.com/api/webhooks/...

# Optional: link discord-infra to monitors created manually in the UI (comma-separated)
# KUMA_MONITOR_NAMES=Traefik HTTP,Portainer,Dozzle,ScriptGold API,ScriptGold Admin
